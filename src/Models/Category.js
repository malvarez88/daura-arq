export default class Category {
    constructor(category) {
        this.id = category.id;
        this.category = category.category;
        this.categoryColor = category.categoryColor;
        this.categoryTextColor = category.categoryTextColor;
        this.dicKey = category.dicKey;
    }
};